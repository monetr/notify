// Mirrors src/constants.ts test-relevant timings. Tests use these to wait past animation/transition
// durations without coupling to the implementation's exact values.
export const ANIMATION_DURATION = 350;
export const SWIPE_EXIT = 350;
export const SHORT_AUTO_HIDE = 300;
